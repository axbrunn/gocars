{{define "nav"}}
<nav>
    <div class="content">
        <div class="logo">
            <a href="/">{{.Title}}</a>
        </div>
        <div class="links">
            <a href="/">Home</a>
        </div>
    </div>
</nav>
{{end}}

{{define "header"}}
<header class="lilac">
    <div class="content">
        <h1><a href="/">{{.Title}}</a></h1>
        <p class="intro-text">Welcome to our dealership.</p>
    </div>
</header>
{{end}}
